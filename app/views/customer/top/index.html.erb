<!DOCTYPE html>
<head>
<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jqueryui/1/jquery-ui.min.js"></script>

<script>
  showExplanVegetable = function() {
	

  	$("#explanvegetable").dialog({
      modal: true,
      resizable: true,
      maxHeight: 500,
      maxWidth : 500,
      minHeight: 150,
      minWidth : 150,
      height: 850,
      width : 900
    });
  };
</script>
</head>
<%= render 'explan' %>
<%= render 'ads' %>
